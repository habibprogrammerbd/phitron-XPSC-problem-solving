#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // pair.............................

    // pair<string, int> student;
    // // student = make_pair("habib",10);
    // student = {"habib", 20};
    // auto [name, roll] = student;
    // // cout << student.first << " " << student.second << endl;
    // name = "korim";
    // cout << name << " " << roll << endl;

    // vector_pair.............................................
    // int n;
    // cin >> n;
    // pair<string, int> students[n];
    // for (int i = 0; i < n; i++)
    // {
    //     // string a; int b;
    //     // cin >> a >> b;
    //     // students[i].first = a;
    //     // students[i].second = b;
    //     cin >> students[i].first >> students[i].second;
    // }

    // for (auto [x, y] : students)
    // {
    //     cout << x << " " << y << endl;
    // }

    // pair nexted /// multupay value dukanor jono pair ke nexted kora ....................

    // tuple......................................................................................


    // pair<string,pair<int,string>> student;

    // student = {"habib",{1,"01740600196"}};

    // cout << student.first << " " << student.second.first << " " << student.second.second << endl;













    // tuple<string, int, string> student;
    // // student = {"habib",1,"01740600196"};
    // student = make_tuple("habib", 30, "01850078960");

    // auto [name, roll, number] = student;

    // // cout << get<0>(student) << " " <<  get<1>(student) << " " << get<2>(student)<< endl;

    // cout << name << " " << roll << " " << number << endl;

    return 0;
}