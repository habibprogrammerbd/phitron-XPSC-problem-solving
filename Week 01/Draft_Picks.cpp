#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        vector<int> v(n);
        string s;
        reverses.begin(), s.end());
        for (int i = 1; i <= 7; i++)
        {
            s = s + to_string(i);
        }
        // for (int i = 0; i < 100; i++)
        // {
        //     if(i)
        // }

        cout << s;
    }

    return 0;
}